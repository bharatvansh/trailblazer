name: CI Build and Test

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'
        cache: 'gradle'
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build with Gradle
      run: ./gradlew build --no-daemon --stacktrace
      continue-on-error: true
      
    - name: Run API tests (always)
      run: ./gradlew :trailblazer-api:test --no-daemon --stacktrace
      
    - name: Run Plugin tests
      run: ./gradlew :trailblazer-plugin:test --no-daemon --stacktrace
      continue-on-error: true
      
    - name: Generate JaCoCo coverage reports
      run: ./gradlew jacocoTestReport --no-daemon
      continue-on-error: true
      
    - name: Upload API coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        files: ./trailblazer-api/build/reports/jacoco/test/jacocoTestReport.xml
        flags: api
        name: trailblazer-api
        fail_ci_if_error: false
      continue-on-error: true
      
    - name: Upload Plugin coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        files: ./trailblazer-plugin/build/reports/jacoco/test/jacocoTestReport.xml
        flags: plugin
        name: trailblazer-plugin
        fail_ci_if_error: false
      continue-on-error: true
      
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: |
          **/build/reports/tests/
          **/build/test-results/
        retention-days: 7
        
    - name: Upload coverage reports
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: coverage-reports
        path: |
          **/build/reports/jacoco/
        retention-days: 7
        
    - name: Upload build artifacts
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: build-artifacts
        path: |
          trailblazer-api/build/libs/*.jar
          trailblazer-plugin/build/libs/*.jar
          trailblazer-fabric/build/libs/*.jar
        retention-days: 30
        if-no-files-found: warn
        
    - name: Test Report Summary
      if: always()
      run: |
        echo "## Test Results" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        if [ -d "trailblazer-api/build/test-results/test" ]; then
          API_TESTS=$(find trailblazer-api/build/test-results/test -name "*.xml" | wc -l)
          echo "✅ API Tests: $API_TESTS test files found" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ API Tests: No results found" >> $GITHUB_STEP_SUMMARY
        fi
        if [ -d "trailblazer-plugin/build/test-results/test" ]; then
          PLUGIN_TESTS=$(find trailblazer-plugin/build/test-results/test -name "*.xml" | wc -l)
          echo "✅ Plugin Tests: $PLUGIN_TESTS test files found" >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️  Plugin Tests: No results found (may require Paper API availability)" >> $GITHUB_STEP_SUMMARY
        fi
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "Note: Fabric module tests are limited due to Minecraft client dependencies. See TESTING.md for details." >> $GITHUB_STEP_SUMMARY
